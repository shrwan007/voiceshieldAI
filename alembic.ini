# ==============================================================================
# A generic, single database configuration for Alembic migrations.
# ==============================================================================

[alembic]
# Path to migration scripts
script_location = alembic

# Template used to generate migration file names; The default value is %(rev)s_%(slug)s
# file_template = %%(rev)s_%%(slug)s

# Sys.path path, will be prepended to sys.path if present.
# Defaults to the current working directory.
prepend_sys_path = .

# Timezone to use when rendering the date within the migration file
# as well as the filename.
# If specified, requires the python>=3.9 or backports.zoneinfo library.
# timezone =

# Max length of characters to apply to the "slug" field
# truncate_slug_length = 40

# Set to 'true' to run the environment during the 'revision' command, regardless of autogenerate
# revision_environment = false

# Set to 'true' to allow .pyc and .pyo files without a dynamic .py file to be detected
# sourceless = false

# Version location specification; This defaults to alembic/versions.
# Multiple locations can be specified using a newline or comma-separated list.
version_locations = alembic/versions

# The output encoding used when revision files are written from script.py.mako
# output_encoding = utf-8

# Database connection URL (can be overridden by DATABASE_URL environment variable in env.py)
sqlalchemy.url = postgresql://voiceshield:voiceshield_pass@localhost:5432/voiceshield_db


[post_write_hooks]
# Post-write hooks define scripts or Python methods that are run on newly generated revision scripts.
# See http://alembic.zzzcomputing.com/en/latest/tutorial.html#post-write-hooks for details.

# Logging configuration
[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
